[project]
name = "crypto-quant-foundation"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["crypto-quant-domain==0.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/crypto_quant_foundation"]
